#include <bits/stdc++.h>
using namespace std;



int main (void) {
    
    // cout << (5 | 8);
    // cout << endl;
    // cout << (5 & 8);

    int x = 0;
    cout << (~x); // prints -1 due to it being stored in the form of 2's compliment . 

    return 0;
}