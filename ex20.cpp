#include <bits/stdc++.h>
#include <map>
using namespace std;

int main (void) {
    
    map <int, pair <int, int >> hashLength;
    vector<pair<int,int>> v;

    for (auto it : v) {
    }

    return 0;
}