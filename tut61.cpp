#include <bits/stdc++.h>
using namespace std;

// Dynamic memory allocation : 
// It is the job of the programmer to delete the dynamic memory . 
// Memory leak is the inability to delete allocated pieces of memory after losing the address of the memory . (problem arises in servers that have progams running the entire time )

int main (void) {
    
    int * x = new int [1000];


    return 0;
}