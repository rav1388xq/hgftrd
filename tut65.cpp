#include <bits/stdc++.h>
using namespace std;

int main (void) {
    /*
        In most of the cases , the time for insertion in a vector is O (1) but in some cases, the time 
        is O (n) because once the vector is full the entire is vector is copied to another loacation with double size and the previous
        memory is freed up . 

        However , taking in account all these thee total time taken for insertion operation is still O (1).
    */



   return 0;
}